Deploys a resource group containing a Log Analytics Workspace
Switched from a local state file to a state file stored in an Azure storage container
Now using shell scripts to define variables for each prod and dev environments, performing a terraform init, and passing on the shell command, ie: "terraform plan"
